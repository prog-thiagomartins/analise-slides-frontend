import { FiLayers } from 'react-icons/fi';

export default function Modelos() {
  return (
    <div className="flex flex-col items-center justify-center h-full p-8">
      <FiLayers className="text-primary-600 text-5xl mb-4" />
      <h1 className="text-2xl font-bold text-primary-700 mb-2">Modelos</h1>
      <p className="text-gray-500">Em breve você verá os modelos disponíveis para análise.</p>
    </div>
  );
}
