export default function Header() {
  return (
    <header className="w-full h-16 bg-white border-b border-gray-200 flex items-center px-6 shadow-sm">
      <div className="font-bold text-xl text-primary-700">
        Finance Analytics
      </div>
      {/* Adicione ações, avatar, etc. */}
      <div className="ml-auto flex items-center gap-4">
        {/* Placeholder para ações */}
      </div>
    </header>
  );
}
